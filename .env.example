# URL pública del sitio (Open Graph / canonical). En GitHub Pages:
# https://TU_USUARIO.github.io/TU_REPO/
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Solo para build local que imita GitHub Pages (sitio de proyecto).
# Nombre del repo sin slashes, ej: mapajugar → rutas bajo /mapajugar
# En Windows (Git Bash) no uses "/mapajugar" con slash inicial.
# BASE_PATH=mapajugar
